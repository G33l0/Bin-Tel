# Bin-Tel — personal BIN list
#
# This file is the source of truth for the database. Add rows here, rebuild,
# and the application is looking at the new data:
#
#     python -m app.cli rebuild
#
# or, in the app: Database → Rebuild from BIN list.
#
# Only `bin` and `bank` are required — the two columns below are a complete,
# working file on their own. Just add a line per BIN:
#
#     414720,Chase Bank
#
# Lines starting with # are ignored, so you can keep notes in here.
#
# To record more, add the columns you want to the header line, in any order.
# Every one of these is optional:
#
#   bin            6 to 8 digits. 8-digit BINs are stored as 8-digit BINs —
#                  they are never shortened to 6 and never invented from a 6.
#   bin_high       for a range: the last BIN in it (leave blank for a single BIN)
#   range_type     issuer_range | account_range | product_range
#   relationship   issuer (default) | former_issuer | program_issuer |
#                  subsidiary | associated_institution
#   effective_from / effective_to
#                  YYYY-MM-DD. A row with an end date describes a *former*
#                  issuer, and Bin-Tel reads it that way.
#
# A BIN may appear on SEVERAL rows — that is how the interesting cases are
# recorded, and all of them are kept.
#
#   Two banks both using one BIN now. Both are named in the result; neither is
#   chosen over the other, and the result is marked Conflicted:
#
#     520001,Harbor Mutual
#     520001,Pacific Savings
#
#   One bank replacing another. Give the outgoing one relationship
#   `former_issuer` and an `effective_to`. The result names the successor as
#   the current issuer and reports the predecessor as stopped on that date.
#   (Columns beyond `bank` are left blank here for readability.)
#
#     bin,bank,relationship,effective_from,effective_to
#     530001,Old Bank,former_issuer,2019-01-01,2024-06-30
#     530001,New Bank,,2024-07-01,
#
#   A BIN nobody uses any more: a former_issuer row with no successor. The
#   result says "No current issuer recorded" and names who stopped, and when.
#   A former issuer is NEVER presented as the current one.
#
# Only a row repeating the SAME bank, relationship and period is a correction.
#
# A whole dataset is easier to keep as its own file than to merge into this
# one. Put it in a `bin-lists` folder beside this file and it is read too,
# with its own columns and its own delimiter — tab-separated is fine, and so
# are the column names it already has (`Issuer`, `Emetteur`, `isoCode2`,
# `Marque`, `Niveau` and others all resolve).
#
# If a list has been through a spreadsheet, its BIN column may have lost its
# leading zeros: 042410 comes back as 42410. Bin-Tel refuses anything under
# six digits rather than guess, and tells you what the value may have been.
# Rebuild with --pad-short-bins once you are sure that is what happened.
#
# Padding cannot fix everything: an 8-digit BIN starting 00 survives as six
# digits and looks like an ordinary 6-digit BIN. Where you can, re-export the
# source with the BIN column formatted as Text and none of this arises.
#
# Never put a full card number, CVV, PIN or cardholder name in this file.
# The reader refuses anything longer than 8 digits for exactly that reason.
#
bin,bank
